[package]
name = "spanda-vision-pro"
version = "0.1.0"
description = "Apple Vision Pro spatial session provider"
license = "Apache-2.0"

[safety]
level = "hardware"

[capabilities]
required = ["spatial.session"]

[hardware]
requirements = []
