[package]
name = "spanda-thread"
version = "0.1.0"
description = "Thread border-router IoT adapter (stub)"
license = "Apache-2.0"

[capabilities]
iot = ["telemetry.read", "command.send", "shadow.sync"]
