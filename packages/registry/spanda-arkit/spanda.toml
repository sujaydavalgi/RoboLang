[package]
name = "spanda-arkit"
version = "0.1.0"
description = "Apple ARKit spatial session provider"
license = "Apache-2.0"

[safety]
level = "hardware"

[capabilities]
required = ["spatial.session"]

[hardware]
requirements = []
