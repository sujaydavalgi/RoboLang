[package]
name = "spanda-arcore"
version = "0.1.0"
description = "Google ARCore spatial session provider"
license = "Apache-2.0"

[safety]
level = "hardware"

[capabilities]
required = ["spatial.session"]

[hardware]
requirements = []
