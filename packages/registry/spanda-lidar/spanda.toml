[package]
name = "spanda-lidar"
version = "0.1.0"
description = "Automotive LiDAR sensor adapters (stub)"
license = "Apache-2.0"

[safety]
level = "sensors"

[capabilities]
required = [ "sensors.lidar.read" ]

[hardware]
requirements = []
