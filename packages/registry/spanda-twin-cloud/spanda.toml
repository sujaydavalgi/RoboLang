[package]
name = "spanda-twin-cloud"
version = "0.1.0"
description = "Twin Cloud SaaS sync for digital mission twins"
license = "Apache-2.0"

[safety]
level = "robotics"

[capabilities]
required = [
  "cloud.invoke",
]

[adapter]
provides = ["twin.cloud"]
requires = ["cloud.invoke"]

[hardware]
requirements = []
