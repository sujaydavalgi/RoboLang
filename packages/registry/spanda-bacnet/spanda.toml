[package]
name = "spanda-bacnet"
version = "0.1.0"
description = "BACnet building automation adapter (stub)"
license = "Apache-2.0"

[capabilities]
iot = ["telemetry.read", "command.send", "point.discover"]
