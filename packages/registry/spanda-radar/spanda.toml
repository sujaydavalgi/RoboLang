[package]
name = "spanda-radar"
version = "0.1.0"
description = "Automotive radar sensor adapters (stub)"
license = "Apache-2.0"

[safety]
level = "sensors"

[capabilities]
required = [ "sensors.radar.read" ]

[hardware]
requirements = []
