[package]
name = "spanda-magic-leap"
version = "0.1.0"
description = "Magic Leap spatial session provider"
license = "Apache-2.0"

[safety]
level = "hardware"

[capabilities]
required = ["spatial.session"]

[hardware]
requirements = []
