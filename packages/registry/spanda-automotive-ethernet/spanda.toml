[package]
name = "spanda-automotive-ethernet"
version = "0.1.0"
description = "Automotive Ethernet SOME/IP and DoIP (stub)"
license = "Apache-2.0"

[safety]
level = "sensors"

[capabilities]
required = [ "automotive.ethernet.connect" ]

[hardware]
requirements = []
