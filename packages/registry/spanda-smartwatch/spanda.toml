[package]
name = "spanda-smartwatch"
version = "0.1.0"
description = "Smart watch wearable telemetry bridge (HealthKit / Health Connect stub)"
license = "Apache-2.0"

[safety]
level = "hardware"

[capabilities]
required = ["wearable.telemetry"]

[hardware]
requirements = []
