# spanda-fleet

Fleet peer relay agents, remote registry, and mesh coordinator for Spanda.

Extracted from `spanda-core` for the lean-core architecture. Program-level fleet orchestration remains in core until the compiler split.

Used by the `spanda-fleet` official package.
